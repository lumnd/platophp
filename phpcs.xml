<?xml version="1.0"?>
<ruleset name="platophp">
    <description>
        The conventions themselves live in lumnd/plato-coding-standard, which the framework and
        every application built on it share. This file holds only what is this repository's own:
        which directories to check, and the two files the 120 column limit does not fit.
    </description>

    <file>src</file>
    <file>bin</file>

    <rule ref="PlatoPHP"/>

    <!--
        These two build their output out of HTML, CSS and JavaScript held in PHP strings. Splitting
        a stylesheet to fit 120 columns makes it harder to read, not easier.
    -->
    <rule ref="Generic.Files.LineLength.TooLong">
        <exclude-pattern>src/debug/profiler.php</exclude-pattern>
        <exclude-pattern>src/debug/error_handler.php</exclude-pattern>
    </rule>
</ruleset>
